# 🔍 AWS EC2 인스턴스 유형별 API 성능 및 임계치 비교 분석

## 💡 주제 선택 배경

클라우드 인프라 운영 시, 단순 사양 비교를 넘어 실제 서비스 워크로드에 최적화된 인스턴스를 선택하는 것이 비용 및 성능 효율화의 핵심입니다. 본 실험은 증권 시스템과 같이 CPU 연산(해시 연산)과 메모리 I/O(시세 조회)가 빈번한 환경을 가정하여, EC2 인스턴스 패밀리별 임계 성능을 실증적으로 파악하고자 진행되었습니다.

## 🎯 실험 배경 및 목적

1. **인프라 튜닝 효과 검증**: 커널 파라미터 및 WSGI(Gunicorn) 최적화 상태에서의 안정성 확인
2. **인스턴스 패밀리별 특성 식별**: 동일 vCPU 대비 Compute(C), General(M), Memory(R) 타입의 응답성 차이 분석
3. **수직 확장성(Scalability) 평가**: 인스턴스 사양 업그레이드(Large → xLarge) 시 성능의 선형적 증가율 측정
4. **부하 시나리오별 대응력 분석**: 단순 집중 부하와 복합 워크로드 상황에서의 시스템 자원 활용도 비교

## 📝 실험 개요

- **대상 인스턴스**: `c6i`, `m6i`, `r6i` (각 Large 및 xLarge 사이즈)
- **서버 환경**: Python Flask + Gunicorn (gthread 모드 활용)
- **부하 툴**: k6 (JavaScript 기반 부하 시뮬레이션)
- **주요 측정 지표**:
    - **실제 RPS**: 초당 처리 가능한 요청 수
    - **p95 Latency**: 하위 95% 사용자에게 전달되는 체감 응답 속도
    - **Max Connecting**: 서버 연결 시 발생하는 최대 지연 시간 (네트워크/백로그 병목 지표)

## 🛠️ 실험 환경 및 방법

- **OS**: Amazon Linux 2023 (Kernel 6.1)
- **배포 모델**: Gunicorn 워커 수 및 스레드 최적화 설정을 통한 멀티코어 성능 극대화
- **네트워크 최적화**: OS 커널의 백로그 사이즈(`somaxconn`) 확장을 통해 연결 유실 최소화

## 💻 사용한 코드 및 시나리오

- server.py
    - 서버가 받는 부하의 종류에 따라 하드웨어 자원이 어떻게 반응하는지 확인하기 위해 설계
    - 시나리오 1: 시세 조회 (`GET /quotes/<symbol>`)
        - **검증 대상:** **Memory 및 Network Latency**
        - **동작 방식:** 100개의 주식 데이터를 메모리(`quotes` 딕셔너리)에 미리 로드해두고 즉시 반환합니다.
        - **특징:** `time.sleep(0.01)`을 통해 미세한 지연을 주어, 동시 접속자가 많을 때 메모리 접근 속도와 I/O 처리 능력을 테스트합니다. 메모리 성능이 좋을수록 초당 처리량(RPS)이 높게 나타납니다.
    - 시나리오 2: 주문 생성 (`POST /orders`)
        - **검증 대상:** **CPU 연산 능력 (Compute)**
        - **동작 방식:** 주문이 들어오면 `hashlib.sha256` 연산을 5,000번 반복합니다.
        - **특징:** 실제 체결 알고리즘이나 암호화 과정에서 발생하는 CPU 부하를 시뮬레이션합니다. CPU 클럭 속도가 높거나 코어 성능이 좋을수록 이 API의 응답 시간(Response Time)이 단축됩니다.
        - gunicorn으로 실행
- 테스트 시나리오 1 - stress_max_push.js
    - 설계 의도 (Design Intent)
        
        이 스크립트의 핵심은 "일정한 압박(Constant Pressure)"입니다. 단순히 사용자를 늘리는 것이 아니라, 서버가 초당 처리해야 하는 요청 수(RPS)를 고정하여, 특정 부하 상황에서 서버 자원(CPU)이 어떻게 유지되는지 관찰하는 것이 목적입니다.
        
    - 주요 메커니즘 설명
        - Constant Arrival Rate (고정 도착률 제어)
        - 일반적인 테스트가 '사용자 수'를 기준으로 한다면, 이 코드는 '초당 요청 횟수(rate)'를 기준으로 합니다.
        - **목적:** 서버가 특정 RPS(예: 700)를 견디면서 CPU 이용 정도를 확인하기 위함입니다.
        - **장점:** 서버의 응답 속도가 느려지더라도 k6는 설정된 `rate`를 유지하기 위해 가상 사용자(VU)를 자동으로 추가 투입합니다.
- 테스트 시나리오 2 - mixed_load.js
    - 설계 의도 (Design Intent)
        - 실제 운영 환경에서는 사용자가 주문만 하거나 조회만 하지 않습니다. 이 스크립트는 읽기(Read)와 쓰기(Write)의 비율을 조정하여 서버의 전체적인 균형을 테스트합니다.
        - **실제 사용자 패턴 모사:** 주식 시장에서 일반적으로 조회 빈도가 주문 빈도보다 훨씬 높은 특성을 반영했습니다.
        - **리소스 경합 확인:** CPU를 많이 쓰는 '주문'과 메모리/네트워크를 주로 쓰는 '조회'가 동시에 발생할 때, 서버가 자원을 어떻게 효율적으로 분배하는지 확인합니다.

## 📈 테스트 시나리오 1: stress_max_push.js 결과

### **1. 인스턴스 유형별 성능 비교 (Family 간 비교)**

**📌 xLarge 사이즈 비교 (4 vCPU)**

xLarge 환경에서는 서버의 처리 능력보다 테스트 클라이언트(k6)의 가상 사용자(VU) 설정 임계치에 먼저 도달하여 RPS 상한선(약 664~665)이 유사하게 관찰되었습니다.

| **인스턴스 유형** | **실제 RPS** | **p95 Latency** | **Max Connecting** | **비고** |
| --- | --- | --- | --- | --- |
| **c6i.xlarge** | 664.5/s | 307.8ms | 29.6ms | CPU 집약 연산 시 우세 |
| **m6i.xlarge** | 665.9/s | 306.3ms | 29.6ms | 연결 병목 발생 가능성 확인 |
| **r6i.xlarge** | 664.5/s | 308.3ms | 17.4ms | 연결 지연 시간(Max Connecting) 가장 안정적 |

<img width="854" height="473" alt="image" src="https://github.com/user-attachments/assets/56a9df99-953f-4919-8a2a-7937ecb82434" />


---

**📌 Large 사이즈 비교 (2 vCPU)**
xLarge에서 발생한 클라이언트 측 병목을 배제하고 인스턴스 본연의 성능 차이를 확인하기 위해 자원을 절반으로 낮추어 테스트했습니다.

| **인스턴스 유형** | **실제 RPS** | **p95 Latency** | **Max Connecting** | **비고** |
| --- | --- | --- | --- | --- |
| **c6i.large** | 314.4/s | 655.4ms | 67.9ms | 연산 부하 시 Latency 증가폭이 큼 |
| **m6i.large** | 335.1/s | 615.43ms | 26.6ms | 전반적으로 고른 성능 유지 |
| **r6i.large** | 327.2/s | 627.4ms | 35.0ms | 동일 코어 대비 c6i보다 우수한 결과 |

<img width="854" height="473" alt="image" src="https://github.com/user-attachments/assets/7c16747d-d35a-4337-8071-cc469b5e9f94" />


### **2. 수직 확장성(Scalability) 분석 (Large vs xLarge)**

**📌 c6i 시리즈 비교**

| **인스턴스 유형** | **실제 RPS** | **p95 Latency** | **Max Connecting** | **비고** |
| --- | --- | --- | --- | --- |
| **c6i.xlarge** | 664.5/s | 307.8ms | 29.6ms | 사양 향상 시 응답 지연 50% 이상 개선 |
| **c6i.large** | 314.4/s | 655.4ms | 67.9ms | vCPU 절반 감소 시 성능 약 50% 하락 |

<img width="699" height="473" alt="image" src="https://github.com/user-attachments/assets/a860b7a5-01fe-4124-b78c-702237b444a5" />


---

**📌 m6i 시리즈 비교**

| **인스턴스 유형** | **실제 RPS** | **p95 Latency** | **Max Connecting** | **비고** |
| --- | --- | --- | --- | --- |
| **m6i.xlarge** | 665.9/s | 306.3ms | 29.6ms | 범용 성능의 높은 확장성 입증 |
| **m6i.large** | 335.1/s | 615.43ms | 26.6ms | vCPU 절반 감소 시 성능 약 50% 하락 |

<img width="699" height="473" alt="image" src="https://github.com/user-attachments/assets/f1026e54-36bc-4a23-994f-9516daebfb61" />


---

**📌 r6i 시리즈 비교**

| **인스턴스 유형** | **실제 RPS** | **p95 Latency** | **Max Connecting** | **비고** |
| --- | --- | --- | --- | --- |
| **r6i.xlarge** | 664.5/s | 308.3ms | 17.4ms | 부하 증가 시 연결 지연이 가장 낮음 |
| **r6i.large** | 327.2/s | 627.4ms | 35.0ms | vCPU 절반 감소 시 성능 약 50% 하락 |

<img width="699" height="473" alt="image" src="https://github.com/user-attachments/assets/01754cd5-a38c-44c0-ae7c-dd9db4affff7" />


> **⚠️ 시나리오 1의 한계**: 단일 경로에 대한 집중 부하는 서버의 하드웨어 성능을 측정하기에는 적합하지만, 실제 사용자의 다양한 경로 접근과 VU 증가에 따른 가변적 지연을 반영하기 어렵다는 한계가 발견되었습니다.
> 

## 🔄 테스트 시나리오 2: 개선 코드(mixed_load) 결과 비교

*단일 API 부하의 한계를 개선하여 여러 요청을 섞어 보냈을 때의 처리 능력을 m6i.large 인스턴스를 기준으로 비교했습니다.*

### 1. m6i.large 결과 비교

| **시나리오** | **실제 RPS** | **p95 Latency** | **Max Connecting** | **비고** |
| --- | --- | --- | --- | --- |
| stress_max_push(1번) | 335.1/s | 615.21ms | 26.6ms | 단순 부하 |
| mixed_load(2번) | 433.51/s | 708.37ms | 40.6ms | **처리량 약 30% 향상** (복합 연산 효율) |

<img width="699" height="473" alt="image" src="https://github.com/user-attachments/assets/f926aa8a-7771-4803-a3dd-39d4f8940ac4" />


## 📋 실험 결과 분석 요약

1. **확장성(Scalability)**: 모든 패밀리에서 Large에서 xLarge로 확장 시 RPS는 약 **2.1배 선형적으로 증가**했으며, Latency는 절반 가까이 감소했습니다.
2. **연결 지연 제어**: R 시리즈(r6i)가 부하 상황에서도 `Max Connecting` 수치가 가장 낮게 유지되어, 대규모 동시 접속 시 네트워크 대기열 관리에 가장 유리함을 확인했습니다.
3. **시나리오별 차이**: 복합 워크로드(`mixed_load`) 시 시스템이 CPU와 메모리 자원을 번갈아 활용함으로써 단일 부하 대비 더 높은 최대 RPS를 수용할 수 있음을 입증했습니다.

## 🔍 인스턴스별 특성 요약

- **c6i**: 순수 CPU 연산 능력이 필요할 때 유리하지만, 임계점 도달 시 응답 속도가 급격히 저하됨.
- **m6i**: 전반적인 성능 균형이 뛰어나며, 다양한 API가 혼합된 범용 워크로드에서 가장 효율적임.
- **r6i**: 데이터 조회 및 세션 관리가 많은 환경에서 연결 안정성 측면의 강점이 뚜렷함.

## 🏁 결론

실험을 통해 비즈니스 로직에 따라 최적의 인스턴스가 달라짐을 확인했습니다. 트래픽 발생 시 연결 안정성이 뛰어난 **r6i 시리즈** 혹은 범용적으로 우수한 처리량을 보인 **m6i 시리즈**가 적합한 선택지라고 생각합니다.

## 🚩 한계 및 향후 과제

- **클라이언트 병목 및 분산 부하 테스트 필요**:
    - xLarge 이상의 고사양 인스턴스 테스트 시, 단일 부하 생성기(k6 Generator)의 자원 한계로 인해 서버의 실제 임계치를 완전히 확인하지 못한 한계가 있었습니다.
    - 향후 k6 부하 생성기 자체를 다중화하는 **분산 부하 테스트 환경**을 도입하여 클라이언트 병목 없는 순수 서버 성능 측정이 필요합니다.
- **워크로드 시나리오 다변화 부족**:
    - 개선된 `mixed_load` 시나리오를 통해 복합 연산 효율을 확인했으나, 시간 관계상 모든 인스턴스 패밀리에 대해 정밀한 교차 검증을 수행하지 못했습니다.
    - 인스턴스별로 최적화된 부하 비중을 찾아내기 위해 더 다양한 변수를 적용한 추가 실험이 필요합니다.
- **스파이크 테스트(Spike Test) 수행 계획**:
    - 현재까지는 일정 수준의 부하를 유지하는 상시 트래픽 대응 능력 측정에 집중했으나, 서비스 개장 직후의 급격한 트래픽 폭증 상황을 가정한 **스파이크 테스트**는 아직 미수행 상태입니다.
    - 순간적인 트래픽 폭주 시의 시스템 탄력성과 연결 복구 능력을 검증하기 위한 추가 실험을 진행할 예정입니다.
